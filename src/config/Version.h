#pragma once

// This file is overwritten by GitHub Actions on every tagged release.
// Locally it stays as v0.0.0-local so builds always compile.
// DO NOT edit the FIRMWARE_VERSION line manually — use git tags instead.
#define FIRMWARE_VERSION "v0.0.0-local"
